#include <stdio.h>
/**
 * main - Entry point
 * Description: " A program that prints the alphabet in lowercase"
 * return: always 0 (success)
 */
int main(void)
{
	for(int x = 'a', x <= z, x++)
	{
		putchar(x);
	}
	return (0);
}

