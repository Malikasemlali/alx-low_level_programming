#include <stdio.h>
/**
 * main - Entry point
 * Description: " A program that prints the alphabet in lowercase"
 * return: always 0 (success)
 */
int main(void)
{
	char c ='a';
	while(c=>z)
	{
		putchar("%c \n",c);
		c= c++;
	}
	return (0);
}

